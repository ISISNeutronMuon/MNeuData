apiVersion: v1
kind: Service
metadata:
  name: kafka-event-aggregator
  namespace: epics-test
  labels:
    app.kubernetes.io/name: kafka-event-aggregator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kafka-event-aggregator
  ports:
  - name: metrics
    port: 8484
    targetPort: metrics
    protocol: TCP
