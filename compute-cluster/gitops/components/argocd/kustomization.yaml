apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - https://github.com/argoproj/argo-cd/manifests/cluster-install?ref=v3.4.5

patches:
  - path: server-patch.yaml
    target:
      kind: Deployment
      name: argocd-server
